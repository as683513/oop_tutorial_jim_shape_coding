name = "Jim"
print(len(name))

some_list = ["some", "name"]
print(len(some_list))

#This is polymorphism in action. a single function does now
#knows how to take different types of objects and return a result
#when ran on a string it gives you total amt of characters
#when ran on a list, returns number of items in the list